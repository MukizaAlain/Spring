spring.application.name=car_rentals
spring.datasource.url=jdbc:mysql://localhost:3306/CarRentals_Management_system
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


#Hibernate Properties
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8081


# Hibernate Configuration
spring.jpa.properties.hibernate.id.new_generator_mappings=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Use UUID as primary key generator
spring.jpa.properties.hibernate.id.uuid.generator=org.hibernate.id.UUIDGenerator

############
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=alainmukiza10@gmail.com
spring.mail.password=yhie scfn ljpt vcby
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configuration (add a secure secret key)
jwt.secret=your_secure_secret_key_here
jwt.expiration=86400000

# Remove these lines from application.properties
# CORS Configuration
# spring.mvc.cors.allowed-origins=http://localhost:3000
# spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.mvc.cors.allowed-headers=*
# spring.mvc.cors.allow-credentials=true